{
  "name": "mosaic",
  "version": "0.1.0",
  "private": true,
  "description": "Mosaic - one workspace for all your data sources. Self-hosted table explorer with CRUD, plus SQL-file-configured multi-source query pages.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "mssql": "^12.7.0",
    "sax": "^1.6.1"
  }
}
